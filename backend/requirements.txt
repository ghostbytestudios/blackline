# Pinned to versions with Python 3.14 (cp314) wheels — verified working on Windows.
# Web framework / server
fastapi==0.141.1
uvicorn[standard]==0.52.1

# Data / validation
pydantic==2.13.4
pydantic-settings==2.15.0
SQLAlchemy==2.0.52

# Schema migrations (run in-process against the decrypted in-memory DB)
alembic==1.19.1

# HTTP client for outbound aggregator sync (the only outbound network use)
httpx==0.28.1

# Cryptography & secret protection
cryptography==49.0.0
argon2-cffi==25.1.0

# Dev / quality
pytest==9.1.1
ruff==0.16.2
